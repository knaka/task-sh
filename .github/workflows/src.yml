name: sqldef
on:
  push:
    branches:
      - main
    # tags:
    #   - v*
  # pull_request:
  #   types:
  #     - opened
  #     - synchronize
  #     - reopened
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install xattr
        run: sudo apt-get install -y xattr
      - name: Run
        run: ./task test
